# Junior PPG IPC Election 2026

Minimal TISS-green ranked ballot, connected to the supplied Google Apps Script endpoint.

Install: npm install
Run: npm run dev
Build: npm run build

The UI requires a @stud.tiss.ac.in email and four distinct candidate preferences. The Apps Script remains authoritative for domain, duplicate, candidate and ranking validation.
